name: CI

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Download ngrok
      run: Invoke-WebRequest https://bin.equinox.io/c/4VmDzA7iaHb/ngrok-stable-windows-amd64.zip -OutFile ngrok.zip

    - name: Extract ngrok
      run: Expand-Archive ngrok.zip

    - name: Auth ngrok
      run: .\ngrok\ngrok.exe authtoken $Env:NGROK_AUTH_TOKEN
      env:
        NGROK_AUTH_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}

    - name: Enable TS
      run: Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -name "fDenyTSConnections" -Value 0

    - run: Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

    - name: Set Password for User (runneradmin)
      run: |
        $username = "runneradmin"
        $existingUser = Get-LocalUser -Name $username -ErrorAction SilentlyContinue
        if ($existingUser) {
          $securePassword = ConvertTo-SecureString -AsPlainText "P@ssw0rd!" -Force
          Set-LocalUser -Name $username -Password $securePassword
        }

    - name: Download Statgraphics
      run: Invoke-WebRequest https://402067.fs1.hubspotusercontent-na1.net/hubfs/402067/Downloads19-501/Statgraphics%20Centurion%2019%20Spanish%20X64.msi -OutFile Statgraphics Centurion 19 Spanish X64.msi

    - name: Move statgraphics to Desktop
      run: |
        Copy-Item -Path .\statgraphics.msi -Destination "$env:USERPROFILE\Desktop\statgraphics.msi"

    - name: Create Tunnel
      run: .\ngrok\ngrok.exe tcp 3389
